<div class="row">
  <div class="col-md-12 d-flex align-items-center">
    <h1>Rent history</h1>
    <%= link_to "New Rental", new_rental_path, class: "btn btn-success btn-sm ml-auto" %>
  </div>
</div>

<div class="row">
  <% @book_rent_history_items.each do |rental| %>
      <div class="col-lg-6">
        <%= render partial: "rental_card", locals: { rental: rental } %>
      </div>
  <% end %>
</div>

<br>
